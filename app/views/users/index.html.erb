

    <% if @user1 && @user2 %>
      <h2>How is better?</h2>

      <div class = "imagen_index"><%= link_to image_tag(@user1.image.url), choose_winner_user_path(@user1, other_id: @user2), method: :patch, class: "imagen_index"%></div>
      <div class = "imagen_index"><%= link_to image_tag(@user2.image.url), choose_winner_user_path(@user2, other_id: @user1), method: :patch, class: "imagen_index"%></div>

    <% else %>
      <p>No hay usuario disponibles en este momento.</p>
    <% end %>



